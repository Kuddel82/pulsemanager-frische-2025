// EmergencyProxyOverride.js - Clean production-ready proxy`nconst originalFetch = window.fetch;`nwindow.fetch = function(url, options) {`n  if (typeof url === "string" && url.includes("api.geckoterminal.com")) {`n    const proxyUrl = "/api/geckoterminal-proxy?target=" + encodeURIComponent(url);`n    return originalFetch(proxyUrl, options);`n  }`n  if (typeof url === "string" && url.includes("scan.pulsechain.com/api")) {`n    const proxyUrl = "/api/pulsechain-proxy?target=" + encodeURIComponent(url);`n    return originalFetch(proxyUrl, options);`n  }`n  if (typeof url === "string" && url.includes("deep-index.moralis.io")) {`n    const proxyUrl = "/api/moralis-proxy?target=" + encodeURIComponent(url);`n    return originalFetch(proxyUrl, options);`n  }`n  return originalFetch(url, options);`n};`nconsole.log("🔧 EMERGENCY PROXY OVERRIDE: Active");
